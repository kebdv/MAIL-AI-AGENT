[project]
name = "mail-ai-agent-backend"
version = "0.1.0"
description = "Backend for TRIPLE MS AI Agentic Mail Assistant"
requires-python = ">=3.11"

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["."]
